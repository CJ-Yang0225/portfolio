import { IconType } from "react-icons/lib";

export type Services = {
  Icon: IconType;
  title: string;
  about: string;
};

export type Skills = {
  Icon: IconType;
  name: string;
  level: string;
};
